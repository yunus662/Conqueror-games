#ifndef PACKED_FEATURES_H
#define PACKED_FEATURES_H

// Packed Features module header
// Declares functions for initializing, updating, and cleaning up the packed features module.
bool initPackedFeatures();
void updatePackedFeatures();
void cleanupPackedFeatures();

#endif // PACKED_FEATURES_H
